---
Generated: 2025-01-27 14:30:34
Ticket Number: 65
---

## Title

Create reusable NavigationItem component for menus

## Type

Feature

## Priority

Medium

## Story Points

3

## Labels

- ui-components
- navigation
- accessibility
- reusable

## Git Branch

`feat/workbench-65-navigation-item`

## Description

Create a reusable NavigationItem component in `presentation/components/ui/NavigationItem.tsx` for menu navigation items. The component must follow project conventions and include full accessibility support with proper ARIA attributes and keyboard navigation.

## User Story

As a developer, I want a reusable NavigationItem component so that I can create consistent navigation menus across the application with proper accessibility and keyboard navigation.

## Acceptance Criteria

1. **Component Structure**
   - [ ] NavigationItem component created in `presentation/components/ui/NavigationItem.tsx`
   - [ ] Component follows project conventions (arrow function, export default, `type` for props)
   - [ ] Component accepts props: `href`, `label`, `icon` (optional), `active`, `onClick` (optional), `ariaLabel`
   - [ ] Component uses SCSS variables from `styles/variables/*` for all styling values
   - [ ] Component SCSS file created in `src/styles/components/ui/NavigationItem.module.scss`

2. **Navigation Features**
   - [ ] Uses Link component for internal navigation
   - [ ] Supports external links (when applicable)
   - [ ] Active state: visual indication when current route matches
   - [ ] Icon support (optional): icon before or after label
   - [ ] Click handler support (for custom navigation logic)

3. **States**
   - [ ] Default state: normal navigation item appearance
   - [ ] Hover state: visual feedback on hover
   - [ ] Active state: distinct styling when `active` prop is true
   - [ ] Focus state: visible focus outline
   - [ ] Disabled state (optional): visually distinct, non-interactive

4. **Accessibility**
   - [ ] Proper semantic element (`<nav>`, `<ul>`, `<li>` structure when used in lists)
   - [ ] `role="menuitem"` or `role="navigation"` as appropriate
   - [ ] `aria-label` or `aria-labelledby` for navigation items
   - [ ] `aria-current="page"` when active (for current page indication)
   - [ ] Keyboard navigation: Arrow keys navigate items, Enter/Space activates, Escape closes menu (if in dropdown)
   - [ ] Focus indicators: visible focus outline
   - [ ] Screen reader announcements for active state
   - [ ] Accessibility IDs use centralized utilities from `shared/a11y/`

5. **Styling**
   - [ ] All colors use SCSS variables (no hardcoded values)
   - [ ] All spacing uses SCSS variables
   - [ ] All font sizes use SCSS variables
   - [ ] Uses BEM methodology for class names (kebab-case, `__` for elements, `--` for modifiers)
   - [ ] No inline CSS
   - [ ] No `!important`
   - [ ] Consistent spacing and alignment
   - [ ] Icon and label alignment

6. **i18n Integration**
   - [ ] Label text uses `useTranslation()` hook
   - [ ] Translation keys added to `src/shared/i18n/messages/fr.json`
   - [ ] Translation keys follow naming conventions

7. **TypeScript**
   - [ ] Props typed with `type` (not `interface`)
   - [ ] Explicit return type for component
   - [ ] Proper TypeScript types for all props
   - [ ] No `any` types
   - [ ] Icon prop type: `React.ReactNode` or specific icon type

8. **Component Export**
   - [ ] Component exported as default
   - [ ] Component can be imported from `@/presentation/components/ui/NavigationItem`

## Technical Considerations

- **Location**: `src/presentation/components/ui/NavigationItem.tsx`
- **SCSS**: `src/styles/components/ui/NavigationItem.module.scss`
- **SCSS Variables**: Add navigation-related variables to `styles/variables/colors.scss` and `styles/variables/spacing.scss` if needed
- **Link Component**: Uses the Link component from `@/presentation/components/ui/Link`
- **Accessibility**: Use utilities from `shared/a11y/` for accessibility IDs
- **i18n**: Use `useTranslation()` hook from `@/shared/i18n` for label text
- **Icons**: Use SVG icons or icon library (if project has one)
- **TypeScript**: Strict mode, explicit types, no `any`
- **Clean Architecture**: Component is pure UI, no business logic
- **Conventions**: Arrow function with export default, `type` for props, SCSS variables only

## Definition of Done

- [ ] NavigationItem component created with Link integration and active state
- [ ] Component uses SCSS variables (no hardcoded values)
- [ ] Component follows project conventions (arrow function, export default, `type` for props)
- [ ] Full accessibility support (ARIA attributes, keyboard navigation, active state indication)
- [ ] i18n integration for label text
- [ ] TypeScript types are correct and explicit
- [ ] Component is tested (unit tests in `__tests__/presentation/components/ui/NavigationItem.test.tsx`)
- [ ] SCSS variables added if needed
- [ ] Translation keys added to `fr.json`
- [ ] Code review completed
- [ ] Component is documented (JSDoc comments)
