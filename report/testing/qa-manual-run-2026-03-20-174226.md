---
Generated: 2026-03-20 17:42:26
Report Type: testing
Command: qa-manual-run
---

# QA Execution Report (Phase 2 - Authenticated Session)

## Scope executed

- Manual authenticated checks on production (`https://tribu-nova.vercel.app`)
- Workspace, account preferences, board/tickets, goals, and project settings gate
- Non-billing scope only

## Results summary

- Passed: 13
- Failed: 1
- Skipped/Blocked: 5

## Passed

1. Authenticated workspace loads with project card visible (`🏠 Lesot / Bouchet`).
2. Account page loads with expected profile/security/preferences/session sections.
3. Language change works (`fr` -> `en`) and persists after reload.
4. Workspace reflects language preference after navigation.
5. Board page loads with expected columns and existing ticket.
6. Ticket creation succeeds (new ticket `L-2 QA automated task 2026-03-20` visible on board).
7. Ticket detail drawer opens and editable fields are available.
8. Ticket update succeeds (`status: in-progress`, `priority: high`) with successful `PATCH` request.
9. Ticket column move is reflected in UI (ticket moved from `Todo` to `In Progress`).
10. Goal page loads correctly with empty state before creation.
11. Goal creation succeeds (`QA Goal 2026-03-20`) and appears in goals list (`1 goal`).
12. Project settings route is reachable and correctly gated by plan (Pro-required message shown).
13. Cleanup partially completed: created QA ticket was deleted successfully and no longer appears on board.

## Failed

1. Account preferences theme switch is not fully immediate in production for `light -> dark` from the account page (requires waiting for server round-trip or a page transition). Expected behavior is instant theme application in-place.

## Skipped / Blocked

1. Full goal cleanup via UI (delete control not exposed from list view in tested screen state).
2. Invitation multi-user end-to-end scenarios (requires second controlled account).
3. Role/permission mutation validation across distinct users (requires multi-user setup).
4. Billing/Stripe checkout/portal scenarios (out of requested scope).
5. Account destructive flows (sign-out, account deletion) intentionally not executed to preserve active test session.

## Observations

- Recurrent console warning still present on multiple routes:
  - `The resource .../772ab1e5dfce5d24.css was preloaded ... but not used within a few seconds`
- No blocking runtime exceptions observed during authenticated functional flows.
