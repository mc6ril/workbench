---
Generated: 2026-03-20 17:24:35
Report Type: testing
Command: qa-manual-run
---

# QA Execution Report

## Scope executed

- Automated regression: unit tests + production build
- Manual browser checks on production (`https://tribu-nova.vercel.app`)
- Non-billing scenarios only

## Results summary

- Passed: 13
- Failed: 0
- Skipped/Blocked: 14

## Passed

1. Workspace project visibility fixed in production (project card visible for authenticated member).
2. Protected route guard: `/workspace` redirects to sign-in when no active session.
3. Protected route guard: `/{projectId}` redirects to sign-in when no active session.
4. Public route `/` (landing) loads correctly when signed out.
5. Public route `/legal` loads correctly.
6. Sign-in page loads correctly with expected controls.
7. Sign-up page loads correctly with expected controls.
8. Google OAuth flow starts and redirects to Google auth page.
9. Console: no blocking runtime errors observed during tested flows.
10. API calls for workspace data return 200 (projects stats, reclaimable projects, runtime config, subscriptions).
11. Unit tests: 89/89 suites passed.
12. Unit tests: 845/845 tests passed.
13. Production build: successful.

## Skipped / Blocked (requires credentials, multi-user setup, or external finance validation)

1. Sign in (email/password) full success path (no test credentials provided in-run).
2. Sign out flow verification from authenticated account settings.
3. Account deletion end-to-end.
4. Profile update end-to-end from account page.
5. Theme/language change and persistence checks (requires active authenticated account in same run).
6. Invitation creation and acceptance end-to-end (requires second controlled account flow).
7. Project creation/deletion from UI in this run context.
8. Role change end-to-end from settings with multi-account verification.
9. Ticket creation/edit/delete/comment flows.
10. Objective creation/update/delete/progress flows.
11. Real-time sync checks on two concurrent users.
12. Plan limit enforcement checks (free vs paid) in UI.
13. Stripe checkout/portal/refund policy validation (out of requested scope and environment-sensitive).
14. Superuser full-access validation.

## Notes

- A production database issue was previously confirmed and fixed: missing `projects` RLS policies (`SELECT`, `UPDATE`, `DELETE`).
- Repository migration added to persist this fix: `supabase/migrations/000038_restore_projects_rls_policies.sql`.
